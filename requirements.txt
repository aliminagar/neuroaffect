# Runtime dependencies (Python 3.11)
numpy>=1.26,<2.1
opencv-python>=4.9,<5.0
Pillow>=10.0,<11.0

# Face detection (Stage 1) — MediaPipe BlazeFace. CPU-friendly, no Torch/TF.
# See docs/detection.md for why this over MTCNN / YOLO-face.
mediapipe>=0.10.9,<0.11

# Affect classification (Stage 2) — pretrained ViT FER model from HuggingFace.
# CPU inference; weights (~330 MB) auto-download & cache. See docs/classification.md.
# For a smaller CPU-only torch wheel: pip install torch --index-url \
#   https://download.pytorch.org/whl/cpu
transformers>=4.40,<5
torch>=2.2

# Dev / test (install with: pip install -r requirements.txt)
pytest>=8.0,<9.0
